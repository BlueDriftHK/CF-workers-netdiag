# 安全政策

**NetSight Pro** 致力于为用户提供安全、可靠的网络诊断服务。我们重视安全问题，并欢迎安全研究人员的负责任披露。

---

## 目录

- [支持的版本](#支持的版本)
- [报告安全漏洞](#报告安全漏洞)
- [漏洞披露流程](#漏洞披露流程)
- [安全措施概述](#安全措施概述)
- [安全最佳实践](#安全最佳实践)
- [常见安全问题](#常见安全问题)
- [责任与免责](#责任与免责)

---

## 支持的版本

我们仅对以下版本提供安全更新和漏洞修复：

| 版本 | 支持状态 | 安全更新 | 备注 |
| :--- | :--- | :--- | :--- |
| v4.2 | ✅ 支持 | ✅ 是 | 当前主线版本（极光玻璃拟态 UI） |
| v4.0 – v4.1 | ✅ 支持 | ✅ 是 | 建议升级到 v4.2 |
| v3.x | ⚠️ 有限支持 | ⚠️ 关键修复 | 建议升级到 v4.x |
| v2.x 及以下 | ❌ 不支持 | ❌ 否 | 请尽快升级 |

我们建议所有用户始终使用最新版本，以获得最新的安全修复和功能改进。

---

## 报告安全漏洞

如果您发现了安全漏洞，请**不要**在公开的 GitHub Issues 中报告，以免漏洞被恶意利用。

### 报告渠道

请通过以下方式报告安全问题：

- **电子邮件**：`asiacomk@gmail.com`
- **PGP 加密**（可选）：如需加密通信，请联系我们获取 PGP 公钥

### 报告内容

请提供尽可能详细的信息，以便我们快速复现和修复：

```markdown
## 漏洞报告

### 基本信息
- **漏洞类型**：（例如：XSS、SSRF、限流绕过、CSP 绕过等）
- **影响版本**：（例如：v4.0、v4.2）
- **严重程度**：（Critical / High / Medium / Low）

### 复现步骤
1. 访问 [URL]
2. 执行 [操作]
3. 观察 [现象]

### 预期行为
（应该发生什么）

### 实际行为
（实际发生了什么）

### 影响范围
（哪些用户/数据受影响）

### 建议修复
（如有建议）

### 附件
（可选：截图、日志、PoC 代码等）
```

### 响应时间

我们承诺在收到报告后：

| 阶段 | 时间 | 说明 |
| :--- | :--- | :--- |
| 确认收到 | 24 小时内 | 回复邮件确认已收到报告 |
| 初步评估 | 48 小时内 | 评估漏洞严重性和影响范围 |
| 修复计划 | 7 天内 | 提供修复时间表 |
| 修复发布 | 视漏洞严重性而定 | Critical/High 漏洞优先修复 |

---

## 漏洞披露流程

我们遵循负责任的漏洞披露流程：

1. **报告接收**：安全团队收到漏洞报告后，会确认接收并开始评估。

2. **评估与修复**：我们评估漏洞的严重性和影响范围，制定修复方案并开发补丁。

3. **补丁测试**：在私有环境中测试修复方案，确保不引入新问题。

4. **协调披露**：
   - 在修复发布后，我们会公开披露漏洞详情
   - 我们会向报告者致谢（除非报告者要求匿名）
   - 我们会同步更新更新日志和安全公告

5. **用户通知**：通过 GitHub Release Notes 和项目文档通知用户升级。

### 公开披露信息

公开披露将包含：
- 漏洞编号（CVE 如有）
- 漏洞类型和描述
- 影响版本
- 修复版本
- 致谢（如适用）

---

## 安全措施概述

NetSight Pro 内置了多层安全防护机制：

### 1. 请求层防护

| 措施 | 说明 |
| :--- | :--- |
| **双层限流** | 通用 API 60 次/分钟/IP，CPU 测试 3 次/分钟/IP，触发返回 429 + `retry-after` |
| **输入校验** | 所有用户输入经过 `parseInt` 钳制 + 范围限制，防止注入和资源耗尽 |
| **域名白名单** | DNS 代理仅允许 `cloudflare.com`、`google.com`、`github.com`，防 SSRF |
| **参数上限** | speedtest ≤5MB、cpu-test ≤200 万次、concurrent ≤16 路、stream ≤10MB |
| **CORS 配置** | 可通过配置限制允许的来源 |

### 2. 响应层防护

| 措施 | 说明 |
| :--- | :--- |
| **CSP 策略** | 每次响应生成随机 `crypto.randomUUID()` nonce，防止 XSS 攻击 |
| **HSTS** | 强制 HTTPS 连接（`max-age=31536000; includeSubDomains`） |
| **X-Frame-Options** | 防止点击劫持（`DENY`） |
| **X-Content-Type-Options** | 防止 MIME 类型嗅探（`nosniff`） |
| **Referrer-Policy** | 限制引用来源泄露（`strict-origin-when-cross-origin`） |
| **X-XSS-Protection** | 启用浏览器 XSS 过滤器（`1; mode=block`） |

### 3. 数据层防护

| 措施 | 说明 |
| :--- | :--- |
| **KV 类型守卫** | `typeof` 检查防止数据投毒，非法数据直接丢弃 |
| **数据 TTL** | KV 数据 7 天自动过期 |
| **自动清理** | 超出 5 条的旧记录自动删除 |
| **不存储敏感数据** | 仅存储测速结果（速度、colo、asn），不存储用户身份信息 |
| **XSS 转义** | 所有服务端注入的用户可控数据经过 `escapeForJS()` 处理 |

### 4. 外部依赖防护

| 措施 | 说明 |
| :--- | :--- |
| **超时控制** | 所有外部 fetch 设置超时（ipapi.co 4s、Scamalytics 3s） |
| **信号控制** | 使用 `AbortController` / `AbortSignal.timeout()` 取消超时请求 |
| **错误静默** | 外部 API 失败时返回空值或降级显示，不影响主服务 |
| **最小权限** | 仅请求必要的地理位置和欺诈评分数据 |

### 5. 客户端防护（v4.2）

| 措施 | 说明 |
| :--- | :--- |
| **CSP nonce 内联脚本** | 所有 `<script>` 标签携带服务端生成的 nonce |
| **无 eval / 无 innerHTML 注入** | 动态渲染使用模板字符串，数据源为受信后端或公共 API |
| **localStorage 仅存主题偏好** | 不存储任何敏感数据 |
| **外部资源 SRI** | Font Awesome CDN 引入（可配置 SRI hash） |

---

## 安全最佳实践

如果您自己部署 NetSight Pro，我们建议：

### 部署安全

1. **使用最新版本**：始终使用最新的稳定版本（当前 v4.2）
2. **配置限流**：根据您的使用场景调整限流阈值，高流量场景可适当提高
3. **启用 HTTPS**：使用 Cloudflare 提供的 HTTPS（默认启用）
4. **绑定自定义域名**：避免使用公共 `workers.dev` 子域名，减少被扫描风险
5. **启用 Cloudflare WAF**：配合 Bot Management 或 WAF 规则增强防护

### 环境安全

1. **保护 KV 命名空间**：不要在 Dashboard 中公开 KV ID
2. **监控日志**：定期使用 `wrangler tail` 检查异常访问模式
3. **限制 CORS**：如非必要，将 `access-control-allow-origin` 限制为特定域名
4. **定期更新**：关注 GitHub Release，及时获取安全修复

### 代码安全（自行修改时）

1. **输入验证**：所有用户输入必须经过验证和清理
2. **避免 `eval`**：不要使用 `eval()`、`new Function()` 或类似动态执行
3. **保持 CSP nonce**：不要移除或硬编码 CSP nonce 机制
4. **转义输出**：任何用户可控数据注入 HTML 前必须经过 `escapeForJS()` 转义
5. **最小权限**：外部 API 调用仅请求必要数据，设置合理超时
6. **代码审查**：在提交 PR 前进行安全自查（参见 CONTRIBUTING.md 审核检查清单）

---

## 常见安全问题

### 1. 限流绕过
- **风险**：攻击者可能通过大量 IP（僵尸网络）或伪造 `cf-connecting-ip` 绕开限流
- **缓解**：Cloudflare 边缘会覆盖 `cf-connecting-ip`，无法伪造；建议配合 Bot Management 使用
- **状态**：⚠️ 已知限制（分布式攻击），已在文档中说明

### 2. DNS 代理 SSRF
- **风险**：攻击者可能通过 DNS 代理端点访问内网资源
- **缓解**：仅允许白名单域名（cloudflare.com / google.com / github.com），且仅支持 HEAD 请求
- **状态**：✅ 已防护

### 3. CPU 资源耗尽
- **风险**：恶意用户可能高频调用 CPU 测试耗尽 Worker CPU 时间
- **缓解**：独立限流（3 次/分钟/IP）+ 迭代次数上限（≤200 万）+ 参数钳制
- **状态**：✅ 已防护

### 4. 信息泄露
- **风险**：响应头或错误信息可能泄露 Cloudflare 内部信息
- **缓解**：已最小化响应头，错误信息不暴露堆栈或内部路径
- **状态**：✅ 已防护

### 5. XSS 注入
- **风险**：用户可控数据（IP、UA、地理位置）未经转义直接注入 HTML
- **缓解**：所有服务端注入数据经过 `escapeForJS()` 处理 + CSP nonce 限制脚本执行
- **状态**：✅ 已防护（v3.6 修复）

### 6. KV 数据投毒
- **风险**：恶意构造的 JSON 数据可能导致解析错误或原型污染
- **缓解**：`typeof` 类型守卫 + `JSON.parse` 包裹在 try/catch 中 + 7 天 TTL 自动清理
- **状态**：✅ 已防护

### 7. 第三方 API 依赖
- **风险**：ipapi.co 或 Scamalytics 不可用时影响功能
- **缓解**：超时控制（3-5s）+ 静默降级 + 客户端重试机制（最多 3 次）
- **状态**：✅ 已处理（功能降级，不影响主服务）

---

## 责任与免责

### 我们的责任

- 及时响应安全漏洞报告
- 在合理时间内修复已确认的漏洞
- 公开披露漏洞信息（修复后）
- 向报告者致谢（如允许）

### 用户的责任

- 使用最新版本的软件
- 遵守部署和配置的最佳实践
- 负责任地报告发现的漏洞（不公开披露）
- 不将本工具用于任何非法目的

### 免责声明

- NetSight Pro 是一个开源工具，按"现状"提供，不提供任何明示或暗示的保证
- 用户自行承担部署和使用该工具的风险
- 对于因使用本工具而导致的任何直接或间接损失，项目维护者不承担责任
- 本工具仅用于合法的网络诊断目的，禁止用于任何非法活动

---

## 联系方式

| 渠道 | 用途 |
| :--- | :--- |
| `asiacomk@gmail.com` | 安全漏洞报告（首选） |
| [GitHub Issues](https://github.com/BlueDriftHK/CF-workers-netdiag/issues) | 非安全问题、功能请求、讨论 |
| [GitHub Security Advisories](https://github.com/BlueDriftHK/CF-workers-netdiag/security/advisories) | 安全公告 |

---

## 安全更新历史

| 日期 | 版本 | 安全修复内容 |
| :--- | :--- | :--- |
| 2026-07-25 | v4.2 | 21 项代码审查修复（输入校验增强、错误处理完善、可访问性改进）；CSP nonce 机制保持 |
| 2026-07-06 | v4.0 | 增强 CSP 策略，添加 ECH 检测，IP 欺诈评分端点安全加固 |
| 2026-07-04 | v3.6 | 修复 XSS 风险（添加 `escapeForJS`），增强 KV 类型守卫，CPU 独立限流 |
| 2026-06-11 | v3.5 | 优化限流清理机制（按需随机抽样），增强 CSP nonce 随机性（`crypto.randomUUID`） |
| 2026-05-28 | v3.3 | 新增完整安全响应头（5 项），实现 IP 级别限流，WebSocket 心跳 |
| 2026-05-10 | v3.0 | 引入基础安全头配置 |

---

## 相关资源

- [Cloudflare Workers 安全文档](https://developers.cloudflare.com/workers/security/)
- [OWASP Top 10](https://owasp.org/Top10/)
- [CVE 数据库](https://cve.mitre.org/)
- [GitHub 安全通告](https://docs.github.com/en/code-security/security-advisories)
- [Content Security Policy (MDN)](https://developer.mozilla.org/en-US/docs/Web/HTTP/CSP)

---

*最后更新：2026-07-25*
*由 BlueDriftHK 维护*
